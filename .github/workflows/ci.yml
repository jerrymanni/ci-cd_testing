name: CI Pipeline

on:
    push:
        branches:
            - "feature/**"
    pull_request:
        branches:
            - main
            - develop

jobs:
    ci:
        runs-on: ubuntu-latest

        steps:
            - name: Run code
              run: echo "Running CI pipeline..."